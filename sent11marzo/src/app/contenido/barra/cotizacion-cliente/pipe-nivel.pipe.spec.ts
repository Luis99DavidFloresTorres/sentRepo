import { PipeNivelPipe } from './pipe-nivel.pipe';

describe('PipeNivelPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeNivelPipe();
    expect(pipe).toBeTruthy();
  });
});
