import { PipeProyectoPipe } from './pipe-proyecto.pipe';

describe('PipeProyectoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeProyectoPipe();
    expect(pipe).toBeTruthy();
  });
});
