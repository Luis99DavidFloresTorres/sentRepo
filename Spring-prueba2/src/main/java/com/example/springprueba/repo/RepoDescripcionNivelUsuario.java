package com.example.springprueba.repo;

/*import com.example.springprueba.model.descripcionNivelUsuario;
import com.example.springprueba.model.nivelUsuario;
import com.example.springprueba.model.producto;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;

import java.util.List;

public interface RepoDescripcionNivelUsuario extends JpaRepository<descripcionNivelUsuario, Long> {
    @Query(value = "SELECT new com.example.springprueba.model.descripcionNivelUsuario(de.id, de.clase, de.subclase) from descripcionNivelUsuario  de inner join nivelUsuario  ni on de.nivelUsuario.id=ni.id where de .nivelUsuario.id= :id")
    List<descripcionNivelUsuario> encontrarnivel(@Param("id") Long id);
}*/
