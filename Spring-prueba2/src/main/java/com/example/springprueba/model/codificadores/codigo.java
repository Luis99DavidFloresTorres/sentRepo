package com.example.springprueba.model.codificadores;
/*
import com.example.springprueba.model.codigoProducto;
import com.example.springprueba.model.producto;
import com.fasterxml.jackson.annotation.JsonIgnore;
import lombok.Data;

import javax.persistence.*;
import java.io.Serializable;
import java.util.List;


@Entity(name = "codigo")
@Data
public class codigo implements Serializable {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    private String codigo;
    private String nombre;
    @OneToMany(mappedBy = "codigoEntity", cascade = CascadeType.ALL)
    @JsonIgnore
    private List<codigoProducto> codigoProductoList;
}
*/
